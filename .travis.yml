language: php

php:
  - 5.3
  - 5.4
  - 5.5
  - 5.6

before_install:
  - sudo apt-get update

install:
  - sudo apt-get install php5-recode

before_script:
  - pecl install
  - composer install --prefer-dist --dev --no-interaction

script:
  - mkdir -p build/logs
  - vendor/bin/phpunit
